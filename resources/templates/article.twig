<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Blog</title>
        <link rel="stylesheet" href="{{ app.assets_path }}/styles/styles.min.css"/>
    </head>
    <body class="container">
        <h1 class="row"><a href="{{ path('article', {article_id: article.id}) }}">{{ article.title }}</a></h1>
        <h3 class="row">by {{ article.author.username }}, on {{ article.date | date("d/m/Y H:i") }}</h3>
        <p class="row">
            {{ article.content }}
        </p>
    </body>
</html>